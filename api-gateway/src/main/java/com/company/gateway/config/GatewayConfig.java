package com.company.gateway.config;

import org.springframework.context.annotation.Configuration;

/**
 * Gateway Configuration - Simple routing only
 * Routes configured in application.properties
 */
@Configuration
public class GatewayConfig {
    // Routes configuration in application.properties
}

